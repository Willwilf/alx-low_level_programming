#include <stdio.h>

/**
* main - causes an infinite loop
* Return: 0
*/

int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i = 0;
/**
 * while - This is used to stop a loop   while (i < 10)
 *      {
 *       putchar(i);
 * \o/' - 'putchar of this is used to print the message to output'}
*/
	putchar('Infinite loop avoided! \o/');

	return (0);
}
